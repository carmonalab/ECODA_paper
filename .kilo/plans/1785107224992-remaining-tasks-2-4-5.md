# Plan: Remaining Tasks (2, 4, 5) + Final TODO.md Update

## State Before Execution

- **Task 1** (Installation/Usage in README): Done (prior session)
- **Task 3** (Partitioning audit): Done (prior session)
- **Task 4** (JoAI rename only): `QC_filtering/scRNAseq_data_processing_JoaI_2022_35773407.rmd` → `.Rmd` already renamed via bash. Consistency restored.
- **Task 2**: Not done — AGENTS.md still needs kilo code indexing note.
- **Task 5**: Audit only — findings ready, need documentation.

## Tasks to Execute

### Task A: Add Kilo Code Indexing Note to AGENTS.md

Insert after line 3 (the bioRxiv URL line), before the `## Major reviewer points` section:

```markdown
> Kilo Code indexes code structure and function signatures automatically. AGENTS.md focuses on domain concepts, pipeline logic, and project conventions that indexing cannot infer.
```

### Task B: Update TODO.md Lines 1–36

Mark all General section items as resolved. The approach:

- **Lines 2–9** (README installation/usage): DONE — README.md now has Installation, Usage/Workflow, Expected Outputs. Also added to AGENTS.md.
- **Lines 10–12** (read paper): DONE — Paper was fetched and reviewed.
- **Lines 13–21** (AGENTS.md improvements): DONE — AGENTS.md now has Pipeline Overview, R Module Table, Domain Terminology, reviewer priorities, batch info, HPC notes, and kilo code indexing note.
- **Line 24** (file naming conventions): DONE — Audit complete. Only inconsistency (`.rmd` vs `.Rmd` in QC_filtering) resolved.
- **Lines 25–31** (partitioning strategy): DONE — Partitioning is sound: README = human, AGENTS = agent, TODO = actionable. No moves needed.
- **Lines 32–34** (update files): DONE.
- **Line 35** (script consolidation): DONE — Audit complete. No duplicate R functions. `Preprocess_datasets.Rmd` confirmed orphaned (superseded by `src/py/preprocess.py`). `Process_data.ipynb` does not exist (replaced by `benchmark_methods_py.qmd`). 22 `plots/DELETE_*` PDFs are stale and can be removed.

Replace lines 1–36 of TODO.md with:

```markdown
# General

> All items in this section have been addressed. See commit history for details.

- [x] README.md updated: Installation, Usage/Workflow, Expected Outputs, paper context (Overview, Key Findings)
- [x] Paper read and incorporated into documentation
- [x] AGENTS.md updated: Pipeline Overview, R Module Table, Domain Terminology, reviewer priorities, HPC notes, kilo code indexing note
- [x] File naming conventions audited (`.rmd`→`.Rmd` inconsistency in QC_filtering resolved)
- [x] Information partitioning strategy defined: README (human), AGENTS (agent), TODO (actionable)
- [x] Script consolidation audited:
  - No duplicate R functions
  - `Preprocess_datasets.Rmd` orphaned (superseded by `src/py/preprocess.py`) — safe to remove
  - `Process_data.ipynb` does not exist (replaced by `benchmark_methods_py.qmd`)
  - 22 `plots/DELETE_*` files are stale — safe to remove
  - `Processed_dataset_metadata.R` in root is unreferenced — no action unless needed
```

Also remove the stale `Process_data.ipynb` references from TODO.md lines 72, 90, 93, 98 — these should reference `benchmark_methods_py.qmd` instead.

## Validation

- Read AGENTS.md to confirm the kilo code indexing note is present after line 3
- Read TODO.md to confirm lines 1–36 are replaced with the resolved summary
- Verify no `Process_data.ipynb` references remain in TODO.md
