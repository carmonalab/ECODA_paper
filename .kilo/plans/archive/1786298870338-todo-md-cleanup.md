# TODO.md cleanup — remove implemented items, restore lost points

## Context

`TODO.md` (at HEAD `3805890`) was audited against all previous versions in git
history (`7d90dbb` … `HEAD`). Audit conclusions (verified against code):

- **Implemented → remove:** Phase 3.5 (SLURM per-pipeline partition strategy,
  fully in `src/slurm_config.sh:78-105` + used by all 3 benchmark submitters);
  "GloScope on HPC" under *Ideas for later* (done in Phase 3.2); the entire
  Phase 1 section (DONE; preserved in changelog + git history).
- **Lost (dropped in the `0beea53` rewrite, never completed) → restore under
  *Ideas for later* (user decision):**
  1. Gene blacklist before HVG selection in `preprocess.py` (old Step 3c:
     dump `aux/genes.blocklist.rds` → text file, `load_blacklist()` in
     `1.1.1_preprocess.py`, apply before HVG selection — still unimplemented;
     blacklist exists only R-side in `src/utils/pseudobulk.R:73`).
  2. Open design question: batch effect analysis with *and/or without* batch
     correction (early TODO versions: "probably more important to only do WITH
     batch correction"; non-corrected results possibly in paper appendix).
  3. Dropped Phase 4 details: `DESeq2.normalize()` — check `batch_col` is
     implemented correctly and does not get `"Sample"` as batch column;
     ECODA batch-associated CT removal should print a warning naming the
     significant cell types; test each cell type separately vs. global
     variance of composition across batches.

## Edits to `TODO.md`

1. **Remove the whole `## Phase 1 — Complete pipelines src/1–4 (DONE, agent)`**
   section (header + summary paragraph). Keep the intro line's
   "Phase 1 (agent, done) →" reference.
2. **Phase 3:** delete the `3.5 SLURM config` bullet. Add one small pending
   bullet after 3.6 (number it `3.7`): resolve the leftover
   `# TODO: Adapt for specific pipelines` comment on `SLURM_PARTITION`
   (`src/slurm_config.sh:114`) — decide per-stage partitions for stages 2–4
   or drop the comment. Keep 3.1–3.4, 3.6, and the Validation bullet as-is
   (3.1/3.2 still await HPC debug validation).
3. **Ideas for later:** remove `GloScope on HPC` (keep `MOFAcellular;
   cell/sample/annotation counts from h5ad without full load`). Add the three
   lost points above as separate bullets.
4. **Keep everything else unchanged:** Phase 2 checklist, Phase 3.1/3.2
   (incl. CODE COMPLETE + PENDING notes), Phase 3.3/3.4/3.6, Phase 4, Human-
   managed tasks, Keep-draft notes.
5. **Changelog:** append one bullet describing this cleanup (what was removed
   as implemented; what was restored and where; new 3.7 item).

## Constraints

- Only edit `TODO.md` — no source files (slurm_config.sh comment stays
  untouched; 3.7 is tracked in TODO only).
- Do not touch `datasets.json`, README, ARCHITECTURE, AGENTS.

## Validation

- Re-read the edited `TODO.md`: no unchecked/pending point, idea, or draft
  note removed beyond the three user-approved removals (Phase 3.5, GloScope
  idea, Phase 1 section).
- Markdown renders (headings/bullets/checkboxes consistent).
- `git diff -- TODO.md` reviewed before commit.
